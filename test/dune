(executables
 (names test)
 (libraries oUnit cmph))

(alias
 (name runtest)
 (deps
  (file keys-long.txt)
  (file keys-fw-1.buf)
  (file keys-fw.buf))
 (action (run ./test.exe)))

; Local Variables:
; mode: tuareg-dune
; End:
