(jbuild_version 1)

(executables
 ((names (vector test))
  (libraries (oUnit cmph))))

(alias
 ((name runtest)
  (deps
   ((file keys.txt)
    (file keys-long.txt)
    (file keys-fw-1.buf)
    (file keys-fw.buf)))
  (action (run ./test.exe))))
